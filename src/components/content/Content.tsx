import { FC } from 'react';
import { Box } from '@mui/material';

const Content: FC = () => {
    return (
        <Box>Halo</Box>
    );
};

Content.displayName = 'Content';

export default Content;
